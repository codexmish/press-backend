// -----user schema enums

enum ActiveStatus {
  ACTIVE
  DEACTIVE
}

enum Role {
  USER
  AUTHOR
  ADMIN
}

enum PostStatus {
  DRAFT
  PUBLISHD
  ARCHIVED
}

enum CommentStatus {
  APPROVED
  REJECT
}
